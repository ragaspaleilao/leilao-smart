# LeilaoSmart Frontend

## Como rodar (desenvolvimento)

1. Copie `.env.local.example` para `.env.local` e ajuste NEXT_PUBLIC_API_URL se necessário.
2. Instale dependências: `npm install`
3. Rode em dev: `npm run dev`
